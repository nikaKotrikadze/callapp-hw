import React from "react";
import UserTable from "../UserTable/UserTable";

const Layout = () => {
  return (
    <div>
      <h1>User Data</h1>
      <UserTable />
    </div>
  );
};

export default Layout;
